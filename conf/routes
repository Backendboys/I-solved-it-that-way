# Endereço inicial
GET     /                           controllers.HomeController.index

# Login
GET     /login                      controllers.AuthController.index
POST    /login                      controllers.AuthController.auth

# --- PROFESSOR ---
# home professor
GET     /homep                       controllers.ProfController.homeProf
# adicionar contato
GET     /questaoes/new               controllers.ProfController.create
POST    /questaoes                   controllers.ProfController.save

# --- ALUNO ---
# home
GET /homea controllers.AlunoController.homeAluno
# logout
GET     /logout                     controllers.AuthController.logout

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
