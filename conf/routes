# Endereço inicial
GET     /                           controllers.HomeController.start
# Login
GET     /login                      controllers.HomeController.login
# Redirecionar para home
POST     /index                     controllers.HomeController.index(usuario: String)

# --- PROFESSOR ---
# home professor
GET     /home                       controllers.ProfController.homeProf
# adicionar contato
GET     /contatos/new               controllers.ProfController.create
POST    /contatos                   controllers.ProfController.save
# logout
GET     /logout                     controllers.ProfController.logout


# --- ALUNO ---
# home
#GET /home controllers.AlunoController.home

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
