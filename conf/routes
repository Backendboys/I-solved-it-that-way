# Endereço inicial
GET     /                           controllers.HomeController.index

# Login
GET     /login                      controllers.AuthController.index
POST    /login                      controllers.AuthController.auth

# --- PROFESSOR ---
# home professor
GET     /homep                       controllers.ProfController.homeProf
# adicionar contato
GET     /contatos/new               controllers.ProfController.create
POST    /contatos                   controllers.ProfController.save
# logout
GET     /logout                     controllers.ProfController.logout


# --- ALUNO ---
# home
GET /homea controllers.AlunoController.homeAluno

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
